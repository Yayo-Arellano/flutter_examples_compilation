name: flutter_hooks_tutorial
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.14.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
    uses-material-design: true
